# Xyra-Core
Xyra - Visual Desktop for Linux VPS (Windows Client) provides a simple desktop-style interface to manage files on terminal-based servers. Browse directories, edit files, upload via drag-and-drop, and integrate SSH tools in one lightweight dashboard
